spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console

logging:
  level:
    root: INFO
    com.mree: DEBUG
    org.hibernate: INFO


ecommerce:
  costPerDelivery: 4.2
  costPerProduct: 2.2
  fixedCost: 2.99

